# __APP_NAME__

Starter project generated with `create-streamcanvas`.

## Run

```bash
pnpm install
pnpm dev
```

## What you get

- A Next.js app wired to `StreamCanvasProvider`
- A demo `/api/demo` route backed by `@streamcanvas/server`
- Chat thread and widget surface primitives from `@streamcanvas/react`

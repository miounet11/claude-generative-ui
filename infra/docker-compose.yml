services:
  streamcanvas-web:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: streamcanvas-web
    environment:
      HOSTNAME: 0.0.0.0
      NODE_ENV: production
      PORT: "3000"
    ports:
      - "127.0.0.1:3210:3000"
    restart: unless-stopped
